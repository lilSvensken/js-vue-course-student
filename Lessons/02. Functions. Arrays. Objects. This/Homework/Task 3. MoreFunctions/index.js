const moreFunction = (number) => {
    // Начало
const mat = (ary) => {
  let newAry = [];
  for( let a = 0; i < ary.length; i++){
  newAry[a] = ary[a] / num;
  }
  return newAry;
}
return mat;
    // Конец
};

export default moreFunction;